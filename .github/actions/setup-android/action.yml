name: setup-android
description: 'Setup Android'
runs:
  using: composite
  steps:
    - uses: actions/setup-java@v3
      with:
        distribution: 'zulu'
        java-version: '17'
    - uses: gradle/gradle-build-action@v2
      with:
        build-root-directory: android
        arguments: extractAARForNativeBuild
      # debug
      continue-on-error: true
    - run: ls -la android
      shell: bash

    - uses: subosito/flutter-action@v2
    - run: flutter pub get
      shell: bash
